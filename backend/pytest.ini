[pytest]
testpaths = tests
addopts = --cov=src --cov-report=term-missing -v
markers =
    integration: marks tests as integration tests (slower, use real file I/O)
    unit: marks tests as unit tests (fast, no I/O)
